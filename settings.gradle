include 'subprojects/griffon-nuvolaicons-core'
include 'subprojects/griffon-nuvolaicons-swing'
include 'subprojects/griffon-nuvolaicons-javafx'
include 'subprojects/griffon-nuvolaicons-swing-groovy'
include 'subprojects/griffon-nuvolaicons-javafx-groovy'
include 'subprojects/griffon-nuvolaicons-guide'

rootProject.name = 'griffon-nuvolaicons-plugin'
rootProject.children.each { project ->
    int slash = project.name.indexOf('/')
    String fileBaseName = project.name[(slash+1)..-1]
    String projectDirName = project.name
    project.name = fileBaseName
    project.projectDir = new File(settingsDir, projectDirName)
    project.buildFileName = "${fileBaseName}.gradle"
    assert project.projectDir.isDirectory()
    assert project.buildFile.isFile()
}
